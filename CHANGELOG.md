# 更新日志

> 本文件累积记录各版本更新。发版时构建脚本会按版本号自动提取对应段落作为 Release 正文，**请勿删除历史版本条目**。

## v0.18.6

### 新增

- 新增：后台用户列表支持按用户过期状态筛选，可分别查看未过期和已过期用户
- 新增：客户端证书支持批量生成，可按用户组一次为组内全部用户（或勾选部分用户）签发证书，并支持设备绑定（默认最大 3 台设备）
- 新增：客户端证书支持续期，对指定用户已签发的证书顺延一年有效期，无需重新下载安装

### 修复

- 修复：用户账号过期后，已建立的门户和 WebVPN 会话仍可继续使用的问题
- 修复：后台管理用户编辑页面对钉钉用户的编辑限制与 LDAP、企微、飞书用户保持一致
- 修复：修复使用iptables后端时fakedns转发失效的Bug
- 修复：修复客户端证书列表按用户名或用户组筛选时，下划线等特殊字符匹配不准确的问题

### 优化

- 优化：优化fakedns解析速度
- 优化：客户端证书生成和证书列表统一显示“用户名(姓名)”格式，方便识别三方登录用户

## v0.18.5

### 新增

- 新增：WebVPN 网关支持处理预检（OPTIONS）请求，并为通过来源校验的实际请求补充 CORS 响应头；现在可按 WebVPN 应用配置精确的跨域来源，符合来源规则时，内网后端无需额外配置 CORS

### 修复

- 修复：关闭 NAT 转发后，组自定义网段仍尝试下发防火墙规则并产生告警的问题
- 修复：删除用户组后，用户列表里该组名仍残留在部分用户的问题
- 修复：按组名、用户名或姓名筛选时无法匹配的问题
- 修复：在门户已登录的状态下，点击 WebVPN 应用偶尔会被要求再次登录（甚至要登录两次）的问题；即使旧的 WebVPN 免登授权已失效，也会继续使用有效的门户登录状态
- 修复：门户登录后跳转 WebVPN 子域时，改用一次性授权参数完成首次免登，并在兑换后清理地址栏，避免因跨子域 Cookie 未携带而重复登录
- 修复：跨站 iframe 访问 WebVPN 应用时，登录会话能够随接口请求正常携带，避免页面资源已打开但业务接口被判定为未登录
- 修复：跨站接口未登录时不再跳转到登录页面，改为返回浏览器可识别的未登录响应，避免被误报为 CORS 错误
- 修复：跨站接口使用自定义请求头时，预检响应会允许浏览器实际声明的请求头
- 修复：WebVPN 免登授权可能被重复使用的问题；授权兑换成功后会立即失效，且会校验门户登录态，避免跨用户复用
- 修复：WebVPN 代理 JumpServer 等使用 `/ui/` 作为前端入口的应用时错误跳转到 RemLink 管理后台登录页的问题
- 修复：跨站 WebVPN 请求遇到应用无权限、配置错误或后端不可用时，仍返回完整的跨域响应头，浏览器可以正确识别实际错误状态
- 修复：过期、吊销或格式无效的 WebVPN 免登授权会及时清理，避免浏览器反复携带失效授权
- 安全加固：收紧 WebVPN 和管理后台的跨域请求校验，降低伪造来源发起请求的风险；预检请求会校验请求方法和请求头名称
- 安全加固：WebVPN 后端地址仅接受明确的 HTTP/HTTPS 地址，路径白名单按完整路径边界匹配，避免错误放行相似路径
- 修复：WebVPN 会话吊销和审计批处理的失败、停止状态能够正确处理，避免管理操作显示成功但实际未持久化，或停止时丢失尾部审计记录
- 安全加固：WebVPN 不再透出后端自行设置的跨域响应头，避免后端宽松配置影响网关的来源白名单
- 安全加固：WebVPN 反代会过滤后端设置的网关会话 Cookie，并清洗指向后端的 Cookie 域名属性，避免后端响应覆盖网关会话或造成 Cookie 作用域混淆
- 修复：访问日志严格校验 IPv6 报文边界，避免截断或异常报文产生错误记录；修复 TCP 重传、乱序回绕识别和不同用户日志去重隔离
- 修复：TLS 握手被拆成多个 TCP 报文时不再提前判定为 HTTPS；IPv4/IPv6 报文严格校验有效边界，分片非首片不再被当作带端口的 TCP/UDP 报文解析
- 修复：修复前端填写路由条目0.0.0.0/0下发客户端报错的Bug

### 优化

- 优化：系统日志「历史日志」模式打开（或切换日期 / 级别 / 关键字）时，自动定位到最底部（即当天最新日志），无需再手动滚动翻到最后一页
- 优化：系统实时日志和历史日志高亮效果
- 优化：在线用户列表的客户端图标悬停时显示具体客户端型号和版本
- 优化：在线用户列表的登录时间等结构化字段保持稳定宽度，用户名等内容列自动填充剩余空间
- 优化：统一优化用户、审计、WebVPN、证书等列表的弹性列和长文本显示，内容过长时可悬停查看完整信息
- 优化：控制台运行日志（登录、上线、断开、各类认证成功等）中的用户标识，现在统一显示为「账号(昵称)」格式（有昵称时），与后台管理界面保持一致
- 优化：补全各身份源用户同步成功汇总日志
- 优化：安全审计中的访问日志和用户活动日志支持按姓名搜索
- 优化：访问日志识别出 HTTP、HTTPS 或 DNS 后，会自动合并先前同一访问产生的 TCP/UDP 记录，避免同一访问显示为两条日志
- 优化：访问日志在有有效数据时尝试识别 HTTP/HTTPS，减少因 TCP 标志位或数据包顺序导致的应用识别遗漏；审计间隔设为 0 时不再维护无意义的去重缓存
- 优化：访问日志可暂存 TCP 连接开头的少量数据，支持识别被拆分或乱序传输的 HTTP/HTTPS 请求，同时限制缓存大小并自动清理闲置连接
- 优化：访问日志新增对 SSH、FTP、SMTP、IMAP、POP3 等常用协议的识别，无法明确判断时仍保留为 TCP
- 优化：访问日志区分并行连接，限制识别缓存的连接数量和总内存，并主动清理闲置连接，降低高并发或异常流量下的资源占用
- 优化：TCP 连接结束时及时释放识别缓存；访问日志的源地址筛选支持 IPv4 和 IPv6
- 优化：访问日志列表新增源端口列，导出增加源端口、目的端口、IP 协议、访问协议和访问详情

## v0.18.4

### 新增

- 新增：策略管理新增「引用详情」，可查看某条策略具体被哪些用户组、哪些用户引用；并支持在详情中批量将选中的组 / 用户移出该策略
- 新增：系统日志页新增「历史日志」模式，可在配置了日志文件路径的前提下，按日期查看此前每天的系统运行日志，并支持按级别、关键字筛选

### 修复

- 修复：WebAuth 与门户认证失败时，用户活动日志缺失或信息不准确（详情笼统、客户端/操作系统/型号为空等）的问题，现在三端失败日志统一记录并补全客户端信息
- 修复：账号/IP 被锁定时，用户活动日志未能正确显示「账号已被锁定」并区分锁定来源的问题
- 修复：客户端已掉线后，在线用户列表仍显示"在线"的问题
- 修复：通过钉钉等单点登录、且账号尚未同步到本地的用户，在 WebAuth 外部浏览器认证成功后会被错误跳转到门户登录页（无法进入门户）的问题
- 修复：在门户已登录的状态下，点击 WebVPN 应用偶尔会被要求再次登录（甚至要登录两次）的问题
- 修复：用户活动日志按设备类型筛选（如 iOS、安卓）不准确的问题，安卓设备会被错误归类为 iOS
- 修复：用户活动日志在解析异常的客户端信息时会偶发写入失败的问题

### 优化

- 优化：系统信息页顶部卡片新增「运行时长」展示
- 优化：自定义品牌（标题 / 图标）在页面加载即实时生效
- 优化：在线用户、锁定管理、IP 映射、用户访问日志、用户活动日志、门户登录态等处的用户名，统一显示为「账号(昵称)」格式（设有昵称时）
- 优化：系统日志历史日志模式改为滚动加载，浏览长日志时无需翻页、滚动到底自动追加
- 优化：登录因账号/IP 被锁定而失败时，日志按「账号+IP」每分钟最多记录一条，避免前端重试刷屏；正常输错密码仍逐条记录
- 优化：账号/IP 达到锁定时间后会立即自动解除封锁，无需再等待下一次登录尝试才恢复
- 优化：WebAuth、门户产生的日志详情加 [WebAuth] / [门户] 前缀，方便区分认证来源
- 优化：在线升级更新源改为可在「系统设置」里手动选择 Gitee(国内镜像) 或 GitHub（部署在国内的服务端建议选 Gitee），默认 Gitee

## v0.18.3

### 功能

- 新增：虚拟网络类型新增「IPVTAP」模式（第四种，原有 TUN / TAP / MACVTAP 三种不变）。IPVTAP 与 MACVTAP 类似，都是把虚拟网卡挂到服务器主网卡上，但 IPVTAP 工作在三层、多个客户端共享主网卡 MAC 地址，适合客户端数量多、主网卡所在网络对 MAC 数量敏感的场景

### 修复

- 修复：关闭「自动添加全局 NAT」开关后，配置了自定义网段的用户组仍然会被下发地址伪装（MASQUERADE）规则的问题
- 修复：创建/编辑用户组时，若填写的自定义网段与服务器主网卡所在的物理网段相同，在 MACVTAP / IPVTAP 模式下会出现网络冲突、路由混乱的问题
- 修复：在后台删除一个用户组后，用户列表页面仍显示该用户「属于已删除的组」的问题
- 修复：通过企业微信 / 飞书 / 钉钉等单点登录（未同步到本地用户）的用户，在 WebVPN 下既无法访问已授权应用、又会被错误踢回登录页的问题
- 修复：批量删除用户时，若部分用户已被删除（如页面刷新后重复提交），整个操作会被误报为「删除失败」
- 修复：钉钉 / 企微 / 飞书同步用户配置了「拒绝同步的用户」名单后，此前只是「不再更新」已落库的拒绝用户、并不会删除他们；现改为：拒绝名单中的用户若在本地已存在，会在下次同步时被自动删除（或移出对应组）
- 修复：管理后台快速连续切换左侧菜单时，偶尔会出现某个菜单「点不动」、必须刷新页面才能恢复的问题
- 修复：手机端下列表排序的上下箭头会掉到表头标题下方、导致表格排版错位的问题
- 修复：在「页脚内容」中自定义了较长的页脚（如版权 + 备案两组徽章）后，管理后台页脚在手机端因不换行而溢出错位的问题
- 修复：用户组、认证源、策略三个列表页顶部的统计卡片此前只统计「当前页」数据，数据多页时数字不准的问题；现已改为按全量数据统计
- 修复：钉钉同步用户未遍历子部门的问题
- 修复：从门户「忘记密码」邮件点开重置链接后，页面一直显示「未知用户」
- 修复：权限控制（ACL）规则中填写较大的端口范围（如 1-65535）后，保存策略会提示保存失败（数据库字段内容过长）的问题
- 修复：后台「实时日志」页面偶发崩溃（服务端 panic、当前连接被断开）的问题
- 修复：用户列表统计卡片此前只统计「本地用户 / LDAP / 外部用户」，会漏掉通过企业微信 / 钉钉 / 飞书单点登录进来的用户（这些用户不计入任何卡片）；现已把三方登录用户一并计入「外部用户」卡片，四类相加与用户总数一致
- 修复：后台「WebVPN 应用」列表删除应用时报「Id错误」、无法删除的问题
- 修复：在用户量很大的环境下，编辑或删除一个用户组时后台保存/删除操作会长时间「转圈」，但配置其实已经保存成功的问题
- 修复：后台批量删除用户（如一次删几百个）速度很慢、且可能删不完的问题

### 优化

- 优化：移动端及各页横屏/竖屏显示效果
- 优化：自定义网段与主网卡物理网段的重叠校验覆盖 IPv4 与 IPv6
- 优化：网页门户与客户端在动态码（OTP）、短信验证码、二次验证等认证步骤的提示文案与交互保持一致
- 优化：钉钉 / 企微 / 飞书 / LDAP 同步用户时，若接口返回空用户列表（常见于通讯录读取权限不足或应用可见范围未覆盖），不再误判为「同步成功」，也不会清空本地已同步用户，后台会明确记录错误日志便于排查
- 优化：钉钉同步增加通讯录权限探针（调用部门详情接口），权限/可见范围不足时不再静默返回空列表，而是直接报出钉钉原生错误码（如 errcode=60011）及说明，便于定位真实原因；企微、飞书接口本身即透传原生错误码，无需探针
- 优化：后台手动触发「同步用户」后，前端提示改为「正在后台同步，请查看后台日志确认结果」，避免异步任务尚未完成就被误导为同步成功
- 优化：用户列表支持按「类型 / 用户组 / 状态」组合筛选，配合原有关键字搜索
- 优化：后台各列表（用户 / 用户组 / 策略 / 认证源 / 证书 / 在线用户 / 审计日志等）的表格列支持点击表头排序
- 优化：LDAP 同步用户时，读取并写入用户手机号（仅当本地手机号为空时才写入，不会覆盖管理员手动维护的值；邮箱沿用相同逻辑）。兼容不同目录 ：优先取 `telephoneNumber`（标准 AD），为空则回退 `mobile`
- 优化：钉钉支持「定时自动同步用户」（每日 0 点），与企微 / 飞书 / LDAP 保持一致。开启方式：在对应认证源配置窗口中打开「自动同步用户」开关即可，钉钉用户将随每日定时任务自动同步
- 优化：后台「访问审计」列表新增「访问目标」列，优先展示域名（如 www.example.com），无域名时才回退显示为「IP:端口」，并支持按 DNS 协议筛选
- 优化：用户列表统计卡片改为在数据库侧直接聚合，用户量很大时（如几万账号）页面加载更稳、不再一次性把全部用户载入内存
- 优化：用户组 / 策略 / 认证源三个列表页顶部统计卡片，以及删除用户组、WebVPN 应用授权校验等后台操作，统一改为按条件在数据库侧过滤/聚合，不再一次性把整张表载入内存；用户量或数据量大时这些操作响应更快、更稳

## v0.18.2

> ⚠️ **升级提示**：本次将门户登录态 Cookie 从通配域改为精确域名写入，老用户浏览器里若还残留升级前用通配域写入的旧 Cookie，**升级后首次使用请手动清除一次浏览器缓存与 Cookie**（或在门户退出一次后重新登录），之后登录态即可正常工作、不再需要反复清理

### 功能

- 新增：系统设置 - 门户设置中的「页脚内容」支持 HTML。管理员可自定义带链接、加粗等富文本格式的页脚（如「© 2026 某某公司 <a href='https://example.com'>信息技术部</a>」），并内置 `.github-badge` 徽章样式，可直接拼接版权与备案徽章，留空仍回退默认页脚

### 修复

- 修复：自定义上传证书失败的Bug
- 修复：通过企业微信 / 飞书 / 钉钉等单点登录认证成功后，VPN 客户端弹出的「认证成功」完成页固定显示「企业微信认证成功」文案的问题。现已根据当前实际使用的认证方式显示通用的「已成功完成认证」提示，不再误导
- 修复：电脑已安装客户端证书（证书名称不是登录账号）的用户，在网页登录时用户名输入框被强制锁定为证书名称、无法改为用账号密码登录的问题。现仅当存在启用证书登录的用户组时才会读取客户端证书，未启用证书登录时不影响账号密码登录
- 修复：开启流控（流量配额）且在线用户较多时，登录偶发提示「用户名或密码错误」、但立即重试又能登录成功的问题
- 修复：访问审计偶发出现「批量写入失败：database is locked」并丢失部分审计记录的问题
- 修复：门户长时间未操作后再点击 WebVPN 应用，偶发提示「无权限」且无法自动跳转到登录页，必须手动退出重新登录才能恢复的问题（现已支持门户会话仍有效时自动续接，无需重复登录）
- 修复：从门户点击卡片进入 WebVPN 应用时频繁要求重新免登的问题

### 优化

- 优化：数据库读写在并发较高时更稳定，减少因写入冲突导致的偶发异常
- 优化：数据库写入冲突重试逻辑改为统一自动重试，覆盖所有数据库操作
- 安全加固：升级前端 HTTP 客户端（axios）与查询字符串解析库（qs），修复其已知安全漏洞
- 优化：未配置客户端证书登录的用户组时，浏览器直接访问服务器地址不再弹出「请选择证书」对话框；一旦有用户组启用了证书登录，相关连接仍会请求客户端证书（浏览器可能弹出选择框，VPN 客户端不受影响），证书登录功能正常
- 优化：门户 Cookie 改为只写在当前精确域名、不再共享给子域，且退出门户时会一并清除浏览器里残留的 WebVPN 会话 Cookie，门户与 WebVPN 登录态彻底隔离、互不干扰

## v0.18.1

> ⚠️ **升级提示**：本次为重大版本更新，包含大量底层重构与安全加固（WebVPN 会话体系、认证链路、数据库表结构等均做了调整）。**正常情况下直接升级即可，无需人工干预**；但为稳妥起见，升级前仍建议**完整备份数据（数据库与配置文件）**，以防极少数异常情况下数据丢失。升级后请重点验证 WebVPN 应用数据与用户登录是否正常。

### 功能

- 新增：系统设置 - 门户设置新增「允许手机端使用 SSO 认证」开关（默认关闭）。开启后手机端（iOS/Android）可使用企微/飞书/钉钉等单点登录组；手机端浏览器模式仍强制内置，**能否完成扫码取决于终端环境**，请按需评估后开启
- 新增：WebVPN 内网 Web 应用反向代理。管理员可在「系统设置 - WebVPN 应用」中配置内网 Web 站点，用户通过专属子域名（如 app.vpn.example.com）免客户端、直接用浏览器安全访问内网系统
- 新增：WebVPN 独立登录态。用户在门户登录后可一键访问已授权的 WebVPN 应用，访问内网应用时无需重复登录；WebVPN 会话与门户会话完全独立，互不影响、互不覆盖
- 新增：门户首页「WebVPN 应用」入口，集中展示当前用户有权访问的内网应用，点击即可在新标签页打开
- 新增：WebVPN 访问审计。所有通过 WebVPN 访问内网应用的请求（用户、应用、URL、状态码、收发流量、客户端 IP、耗时等）都会被记录，管理员可在「系统设置 - WebVPN 审计」中查询与导出 CSV
- 新增：WebVPN 证书支持。WebVPN 泛域名（`*.你的域名`）证书可通过 Let's Encrypt 一键申请或上传自定义证书，使用独立的「WebVPN 泛域名证书」（与 VPN 主证书分开存储、互不覆盖），配合 SNI 通配匹配为所有子域名提供 TLS 终止
- 新增：WebVPN 会话管理。管理员可在「WebVPN 审计」页按用户名一键踢出该用户的所有 WebVPN 会话，强制其下线重登
- 新增：WebVPN 应用支持「跳过证书校验」。当内网后端使用自签/内网证书（https 但证书不被公开信任）时开启，网关可正常代理访问内网站点
- 新增：WebVPN 登录保持时长可配置。管理员可在「系统设置」中分别设置「会话续期周期」（默认 60 分钟，用户持续访问时自动延续登录态）与「最长登录时长」（默认 8 小时，自首次登录起算，超过后无论是否活跃都需重新登录）。两项均留空（0）时启用上述默认值
- 新增：WebVPN 子域名下可直接退出登录。在浏览器访问子域名下的 `/webvpn/logout` 即可看到退出确认页，点击「确认退出」后会清除本子域的 WebVPN 登录态（不影响门户其它功能），方便用户在子域访问内网应用时主动下线

### 修复

- 修复：取消用户的内网应用访问权限（移出用户组、从应用白名单移除、禁用或删除用户/用户组）后，该用户此前已登录的会话仍可继续访问的问题。现权限变更后会立即令相关用户重新登录，确保权限调整即时生效
- 修复：删除用户（单个或批量）后，该用户此前已登录的 WebVPN 会话仍可继续访问的问题。现删除用户会立即令其 WebVPN 会话失效
- 修复：门户首页「我的应用」会把内网后端地址（如 http://10.0.0.5:8080）暴露给浏览器的问题。现门户仅返回应用名称、说明与访问链接，不再泄露内网地址
- 修复：WebVPN 与门户的会话 Cookie 共享域算法不一致，在特定多段域名下可能导致「门户已登录却仍被要求重新登录」的问题。现两者统一按主域共享登录态
- 修复：在 WebVPN 子域名登录后，门户登录态（portal_session）被一同写入父域共享 Cookie，导致同浏览器在父域门户无法切换到其它账号登录（只能用子域登录的用户）。现子域登录只签发 WebVPN 免登授权、不再写入门户会话，门户登录态与 WebVPN 会话彻底解耦
- 修复：编辑并保存 WebVPN 应用后，会把**所有授权用户**立即踢下线（误吊销会话）的问题。现仅吊销「被移除授权」的用户/组成员，保留与新增授权的用户不受影响
- 修复：服务重启后，此前被「踢出会话」的用户仍能使用旧会话直到自然过期的问题。现踢出记录会被持久化保存，重启后依然有效，确保被踢用户无法用旧会话重新进入
- 安全加固：WebVPN 子域名下对所有门户相关路径（含非浏览方式的登录页请求）统一拦截，杜绝绕过子域隔离访问门户接口越权操作门户账号
- 修复：未登录用户直接访问 WebVPN 子域名（如 nas.vpn.example.com）时，本应跳转到该子域自身的登录页（/ui/#/portal）认证，却跳转到错误的门户地址并直接提示「无权限」、导致始终无法登录的问题。现统一跳回当前子域登录页，登录成功后会自动兑换 WebVPN 会话、无需重复登录
- 修复：用户日志中大量下线记录被笼统记为「用户掉线」，无法区分真实断线与客户端正常重连的问题。现新增「客户端自动重连，旧连接已关闭」「链路异常断开」「虚拟网卡建立失败」三种下线原因，可据此判断是否存在真实的网络质量问题
- 修复：极少数情况下用户下线原因显示错误（如客户端主动断开被记成掉线）的问题
- 优化：WebVPN 长时间运行、接入用户较多时，网关内存占用不再持续增长，避免久运行后访问变慢
- 优化：证书申请页的 Let's Encrypt 申请流程。申请「WebVPN 泛域名证书」时，「证书类型」选择上移到域名之前，域名框自动显示 `*.<WebVPN 域名>` 且不可编辑（由 WebVPN 域名决定，无需手动填写），切换类型时校验提示自动清理，降低误操作
- 修复：在门户退出登录后，有时仍停留在已登录状态、或切换其他账号登录不生效，必须清理浏览器缓存才能恢复正常的问题。现退出门户时会一并吊销该用户的 WebVPN 会话（无论子域残留的旧会话是哪个账号签发的），下次访问子域会用当前/新身份自动免登兑换，不再需要手动去子域退出或由管理员踢会话
- 安全加固：WebVPN 退出登录接口增加同源校验，阻止恶意网页诱导已登录用户强制下线（跨站注销攻击）
- 安全加固：反向代理入站阶段额外清除客户端伪造的 `X-RemLink-WebVpn` 标记头，避免攻击者冒充「来自可信网关」绕过后端鉴权
- 安全加固：WebVPN 应用名称限定为小写字母、数字与中划线，防止作为子域名前缀时拼出非预期主机
- 优化：访问审计队列在突发高并发被打满时，对可疑/高危（4xx/5xx）请求降级为本地日志记录，避免审计盲区
- 安全加固：门户登录成功后的回跳地址（redirect 参数）增加同域校验，仅允许本站相对路径，阻止跳转到外部钓鱼网站
- 修复：未登录用户直接访问 WebVPN 内网应用子域名时无法进行认证的问题
- 修复：WebVPN 子域名登录页不显示第三方登录和短信登录入口的问题。现在子域名登录页与门户登录页一致，完整显示第三方登录（企微/飞书/钉钉）和短信登录入口
- 新增：系统设置 - 门户设置新增「WebVPN 第三方登录专用门户域名」配置项，**仅供 WebVPN 子域名三方登录跳转认证用**，填写门户域名。留空则子域名登录页不显示三方登录入口。修复了子域名三方登录跳转错误域名导致认证失败的问题
- 修复：中途取消某用户的内网应用访问权限后，该用户访问对应子域名会陷入「不停刷新页面」死循环的问题（门户已登录、前端自动回跳、网关又判定未登录再次跳转）。现网关会直接提示「无权访问该应用」，不再反复跳转
- 修复：后台管理 - 认证源编辑/新增页面在夜间（深色）模式下出现大片白色背景、与其它页面风格不统一的问题。现已跟随系统主题自动适配深色风格

## v0.17.3

### 修复

- 修复：钉钉扫码登录失败的问题。修正为新版 OAuth2 接口流程：需在钉钉后台为应用开通通讯录读取权限
- 修复：飞书无法同步用户的问题（新增用户详情接口拉取）
- 修复：飞书扫码登录通过认证后无法建立连接的问题
- 修复：门户第三方登录可能导致无法进门户的Bug
- 修复：钉钉/飞书/企微未配置「允许部门」时无法同步用户的问题，现未配置部门则同步应用权限范围内的全部用户

### 优化

- 优化：钉钉、飞书、企微同步用户时补全邮箱和手机号，并支持「拒绝的用户」名单（同步时自动跳过）
- 优化：SSO 认证源配置页面回调地址提示
- 优化：在线用户列表、门户首页优先显示昵称（姓名），更直观
- 优化：钉钉认证源接入后台「同步用户」操作，可手动触发同步

## v0.17.2

### 功能

- 新增：LDAP 认证源支持「LDAPS 直连」
- 新增：钉钉认证源。同时支持按部门限制登录范围、拒绝指定用户ID、以及定时同步用户到本地
- 新增：飞书认证源支持「拒绝的用户」，可填写被禁止登录的用户ID列表（与允许的部门叠加生效）
- 新增：支持Web 认证「按用户名过滤用户组」，**仅支持本地用户**。开启：先输入用户名、仅展示该用户所属用户组；关闭（默认）：直接展示全部启用组
- 安全加固：新增防暴力破解保护。若同一 IP 短时间内频繁尝试大量不同账号，该 IP 会被自动锁定；当某个真实用户随后成功登录时，锁定自动解除

### 修复

- 修复：锁定管理页面点击单条解锁会把相关记录全部解锁的Bug
- 修复：纯证书认证的用户连接时会多弹一次选组窗口，且窗口不会自动关闭、手动关掉会导致连接断开。恢复为点击连接即自动完成认证
- 修复：「排除本地网络」对IPv6同样生效

### 优化

- 优化：加入防爆日志限流器，高频攻击时防爆日志不再刷屏，避免日志文件被刷爆、占满磁盘。同一条告警最多每分钟记录一次
- 优化：门户登录改用统一认证流程，严格按所属用户组的认证策略执行。
- 优化：后台管理菜单中危险操作的分隔线改为弱化显示，减少视觉干扰
- 优化：门户第三方登录按钮布局——1 个时居中、2 个时并排、3 个时自动缩小一行展示

## v0.17.1-beta

> ⚠️ **升级提示**：本次为重大版本更新，改动较大。升级前请务必**完整备份数据（数据库与配置文件）**，以防升级异常导致数据丢失。

### 功能

- 新增：IPv6 双栈支持。配置 `ipv6_cidr`（如 `2001:db8:1::/64`）后自动为客户端分配 v6 地址（/128），支持 TUN / TAP / macvtap 三种链路模式；新增独立开关「IPv6 全局 NAT（global_nat6）」管控 v6 出网（开启为 NAT66，关闭为纯路由，需上游回指 v6 池路由），与 IPv4 的「全局 NAT」互不干扰，可独立配置「IPv4 NAT 出网 + IPv6 纯路由」；支持 IPv6 明细路由下发。留空则保持纯 v4 行为不变
- 新增：IPv6 双栈 —— v6 精细 ACL 匹配：开启策略 ACL 后，v6 流量按目的地址/端口级二次过滤（与 v4 同机制），不再"过路由即全放"
- 新增：IPv6 双栈—— v6 访问审计：记录 v6 流量的源/目的地址、协议与端口，与 v4 审计同口径（存储 varchar(60) 已够用）
- 新增：IPv6 双栈—— v6 DNS 拦截：v6 DNS 报文支持解析与改写
- 新增：IPv6 双栈—— 组级独立 v6 出网隔离：组配置新增「IPv6 网段」（前端，前缀须小于 128），每组独立 v6 CIDR 分别 NAT66/stateful FORWARD 出网，避免所有组共用一段 v6 出网；留空则复用全局 v6 池
- 新增：组级独立出网网卡——组配置新增「出网网卡」（前端网段配置区，下拉选择）；留空=沿用全局 master_dev，适用于多出口网卡按组分流
- 新增：允许设置IPv6 DNS服务器
- 优化：IP 映射表支持同一终端（MAC）在不同用户组/出口下各自保留独立的 IP 绑定记录。此前跨出口漫游连接时，原出口的 IP 映射会被覆盖、映射表中仅显示最新一条；现各组的绑定分别保留、均可查询。IP 保留（固定 IP）的生效范围调整为「同一组内」，跨组漫游不再因开启了 IP 保留而导致连接失败
- 新增：IP 映射支持多选批量删除
- 新增：IP 映射列表支持筛选——按 IP 地址、MAC 地址、用户名、组/出口、IP 保留状态快速过滤
- 优化：策略编辑页路由条目（包含/排除路由）超过 10 条时默认折叠为「共 N 条」摘要并提供「展开逐条编辑」；条目较多时列表区改为固定高度滚动，避免编辑弹窗被数百条路由撑得过长。ACL 规则区同样在条目过多时启用滚动。

### 修复

- 修复：证书自动认证失败时，无法更换组的Bug
- 修复：企微/飞书认证中文组名乱码
- 修复：部分主机装系统时全局禁用了 IPv6（`disable_ipv6=1`，连 link-local 都没有），导致双栈开启后出网网卡拿不到运营商 v6 地址、v6 完全不工作。现双栈开启时自动解除禁用（`net.ipv6.conf.{all,default,<egress>}.disable_ipv6=0`）
- 安全：IPT 模式下 FORWARD 链由无条件放行收紧为有状态放行（仅放行 VPN 网段出站及其 established/related 回包），与 nftables 行为一致，避免客户端被外部主动入站访问
- 修复：TAP / macvtap 模式下 IPv6 实际不可用——服务端未将 v6 网关地址赋到桥 `remlink0`（TAP），且未对网关及池内其他客户端地址做 NDP 代答（macvtap 因主机隔离无法经内核应答网关 NS）。现 TAP 将 v6 网关 /128 赋到桥、allTapRead 的 NS 代答覆盖网关 / 本会话 / 池内其他客户端三种目标，TAP 与 macvtap 的 v6 数据面完整打通
- 修复：DTLS 解压越界导致服务端 panic——解压后长度 `nn` 未做边界守卫（`nn<=0 || nn+1 > BufferSize` 会越界切片并 panic，可被恶意高压缩比包远程触发）；现与 CSTP 路径对齐，越界时丢弃该包并继续，不中断会话
- 修复：TAP / macvtap 模式独立组段（组配置了自定义 CIDR）未下发 NAT/NAT66 规则——`LinkTap`/`LinkMacvtap` 此前遗漏 `setGroupNAT`，导致组级独立网段流量无法 NAT 出网；现与 TUN 对齐，建链时统一调用 `setGroupNAT`
- 修复：ACL 对非 IPv4/IPv6 的畸形包默认放行——`checkLinkAcl` 的 default 分支原返回放行，存在绕过风险；现改为与「v6 无法解析」分支一致，默认拒绝
- 修复：服务监听地址（VPN 服务 / DTLS / 管理后台）若配成 `0.0.0.0:端口`，在 Linux 上仅监听 IPv4，导致 IPv6 客户端连不上；现将该通配写法归一成 `:端口`（双栈监听，同时接受 v4/v6，对纯 v4 客户端无影响）。仅作用于绑定地址，不影响通告地址
- 修复：组自定义网段 NAT 规则在「仅改出网网卡(out_dev)、网段不变」时不生效（原需重启服务才生效）——现出网网卡变化时自动删除旧规则并按新网卡重新下发，无需重启
- 修复：删除用户组或变更组网段后，该组在防火墙（iptables/nftables）里的 NAT/转发规则残留至整机重启——现组删除及网段变更时按旧网段清理对应规则（nftables 通过规则 UserData 标记精准删除，不影响全局规则；iptables 按源段+接口精准删除）
- 修复：iptables 后端下组自定义 v4 子网的新出站连接被 FORWARD 默认策略丢弃（此前只做了 MASQUERADE 未放行 FORWARD）——现与 nftables 对齐，补充组源段 FORWARD ACCEPT
- 修复：组 IP 网段字段误填 IPv6 CIDR 时后端报错误导；现后端明确提示「必须是 IPv4 CIDR」，前端同步增加 IPv4 格式校验
- 修复：IPv6 出网 NAT 与 IPv4 解耦——原「全局 NAT」为单一开关，关闭它做 v6 纯路由会同时关掉 IPv4 的 NAT（需手动配 v4 NAT）；现拆分为「全局 NAT（IPv4）」与「IPv6 全局 NAT（global_nat6）」两个独立开关，关闭后者不影响 IPv4 NAT
- 修复：排除出口 IP（exclude_export_ip）原先只对 IPv4 生效，IPv6 客户端的出口地址未被排除、仍被加密传输；现补上 v6 分支（优先读 X-Cstp-Remote-Address-Ip6，取不到则回退实际 TCP 连接的 v6 源地址），下发 X-CSTP-Split-Exclude-IP6，IPv4 / IPv6 均生效
- 修复：前端 IPv6 网段前缀允许填 /128（后端拒绝）导致前后端不一致；现前端同步限制前缀须 ≤127
- 修复：组自定义网段未做重叠校验，两个组或组与全局 VPN 池网段重叠会导致路由/NAT 歧义；现保存时拒绝与全局池及其他组的自定义网段重叠
- 修复：组出网网卡(out_dev)指向不存在的网卡时静默下发无效规则；现检测到网卡不存在则告警并跳过，待网卡恢复后下次连接自动重试
- 安全：新建用户时系统自动生成的初始密码不再写入服务日志，避免凭据泄露到日志文件
- 安全：系统备份文件中包含的认证配置（如第三方认证凭据）现以加密形式存储，降低备份文件泄露带来的风险（还原与管理后台编辑不受影响）
- 修复：高并发场景下部分客户端的流量统计数据偶发不准确，现已修正
- 修复：IP 池在全部地址均被占用时复用「最早登录」地址的绑定记录未写入数据库（客户端重连后绑定关系错乱，且已分配的 IPv6 地址会丢失）；同时修复用户名模式（非唯一 MAC）下同一设备崩溃重连时地址分配失败的问题
- 安全：加固统计清理等内部接口的数据库查询，消除潜在的注入风险
- 变更：NAT 出网主网卡配置项 `ipv4_master` 重命名为 `master_dev`；旧键 `ipv4_master` 在持久化配置与环境变量 `LINK_IPV4_MASTER` 中仍兼容，升级无需手动改配置
- 修复：组自定义网段中若将网关地址（ClientGateway）配置在 IP 分配区间内，该网关地址可能被分配给客户端，引发客户端与隧道网关地址冲突；现分配 IP 时自动排除网关地址
- 安全：SSO 登录回调增加 state 一次性消费与来源 IP 绑定，防止认证会话被重放或跨客户端冒用
- 安全：测试用户组 / 认证源登录接口接入防暴力破解（失败锁定），防止凭据被爆破
- 安全：管理后台登录凭据（JWT）支持吊销，登出后立即失效，避免令牌长期可用
- 安全：本地账号认证统一登录失败提示并将密码校验前置，防止通过返回差异枚举用户名是否存在
- 安全：LDAP StartTLS 新增服务端证书校验开关（默认关闭，保持原有兼容行为），可按需开启防中间人攻击；前端 LDAP 配置新增「校验证书」开关，可在界面直接开启
- 修复：密码校验对恰好 60 字节的明文密码兼容判定错误（现按 bcrypt 前缀准确识别哈希与明文）
- 修复：用户编辑页 OTP 开关方向与颜色反了——现修正
- 修复：软件配置页移动端布局优化

## v0.16.3

### 功能

- 优化：原「软件配置」中的企微回调验证文件、LDAP/企微/飞书自动同步开关，移至各自认证源配置窗口，支持每源独立控制自动同步。**升级后请在各认证源配置中重新填写以下字段（旧全局值不再自动迁移，需手动配置）：LDAP / 企业微信 / 飞书的「自动同步用户」开关，以及企业微信的「验证文件名」「验证文件内容」**

### 修复

- 修复：仪表盘实时图表只有「内存使用率」能刷新，在线数 / 网络吞吐 / CPU 三张图不显示实时数据的Bug
- 修复：仪表盘刚启动实时队列为空时 `formatOnline` 访问空数据导致报错弹窗；折线图组件重复 `echarts.init` 不释放实例导致内存泄漏
- 修复：后台长时间空闲后 JWT 过期，重新登录后部分菜单点击无响应、必须手动刷新页面才能恢复的问题

## v0.16.2

### 功能

- 优化：原「软件配置」中的企微回调验证文件、LDAP/企微/飞书自动同步开关，移至各自认证源配置窗口，支持每源独立控制自动同步。**升级后请在各认证源配置中重新填写以下字段（旧全局值不再自动迁移，需手动配置）：LDAP / 企业微信 / 飞书的「自动同步用户」开关，以及企业微信的「验证文件名」「验证文件内容」**

### 修复

- 修复：仪表盘实时图表只有「内存使用率」能刷新，在线数 / 网络吞吐 / CPU 三张图不显示实时数据的Bug
- 修复：仪表盘刚启动实时队列为空时 `formatOnline` 访问空数据导致报错弹窗；折线图组件重复 `echarts.init` 不释放实例导致内存泄漏
- 修复：后台长时间空闲后 JWT 过期，重新登录后部分菜单点击无响应、必须手动刷新页面才能恢复的问题

### 优化

- 优化：强制改密表单统一样式
- 优化：软件配置页面
- 优化：门户刷新动画

## v0.16.1

### 功能

- 新增：用户管理支持「强制修改密码」开关（仅本地用户生效），用户列表新增「需改密」标识
- 新增：门户首次登录强制改密
- 新增：WebAuth 首次登录强制改密内联体验（与门户一致）
- 新增：自定义首页支持与门户共存

### 变更

- 调整：原生 SAML（企微/飞书单点登录） 在移动端强制使用内置浏览器
- 新增：批量导入用户支持「强制改密」列
- 调整：新增用户默认开启「强制改密」；管理员可在新增/编辑时取消勾选
- 调整：强制改密统一仅经 Cisco AnyConnect 内置浏览器完成，openconnect需经由用户门户进行改密

### 修复

- 修复：本地用户首次登录强制改密偶被静默跳过
- 修复：AnyConnect 强制改密后无法进入后续认证步骤（如 local+otp）
- 修复：WebAuth 首次登录强制改密返回「未知响应」无法继续
- 修复：管理后台关闭短信功能时误清空已配置密钥 / 签名 / 模板
- 修复：门户首次登录强制改密后数据库标记未清除，导致每次登录仍被要求改密
- 修复：WebAuth 强制改密提交后数据库标记未清除，导致反复要求改密
- 优化：清理认证 / 改密链路重复查库
- 修复：强制改密提交接口校验会话是否处于强制改密流程，防普通登录会话被滥用为改密入口
- 修复：强制改密提交接口复用全局锁定状态（lockManager）防暴力
- 修复：自定义门户打开快捷链接导致门户崩溃的Bug
- 修复：自定义品牌（Logo/标题/页脚/favicon）同步应用到管理后台、WebAuth 认证页与用户门户
- 修复：管理后台浏览器标题与网站图标未跟随品牌设置
- 优化：自定义 CSS 帮助文档补充 scoped 样式需加 !important 的说明

## v0.15.2

### 功能

- 新增：用户管理支持「强制修改密码」开关（仅本地用户生效），用户列表新增「需改密」标识
- 新增：门户首次登录强制改密
- 新增：WebAuth 首次登录强制改密内联体验（与门户一致）
- 新增：自定义首页支持与门户共存

### 变更

- 调整：原生 SAML（企微/飞书单点登录） 在移动端强制使用内置浏览器
- 新增：批量导入用户支持「强制改密」列
- 调整：新增用户默认开启「强制改密」；管理员可在新增/编辑时取消勾选
- 调整：强制改密统一仅经 Cisco AnyConnect 内置浏览器完成，openconnect需经由用户门户进行改密

### 修复

- 修复：本地用户首次登录强制改密偶被静默跳过
- 修复：AnyConnect 强制改密后无法进入后续认证步骤（如 local+otp）
- 修复：WebAuth 首次登录强制改密返回「未知响应」无法继续
- 修复：管理后台关闭短信功能时误清空已配置密钥 / 签名 / 模板
- 修复：门户首次登录强制改密后数据库标记未清除，导致每次登录仍被要求改密
- 修复：WebAuth 强制改密提交后数据库标记未清除，导致反复要求改密
- 优化：清理认证 / 改密链路重复查库
- 修复：强制改密提交接口校验会话是否处于强制改密流程，防普通登录会话被滥用为改密入口
- 修复：强制改密提交接口复用全局锁定状态（lockManager）防暴力
- 修复：自定义门户打开快捷链接导致门户崩溃的Bug
- 修复：自定义品牌（Logo/标题/页脚/favicon）同步应用到管理后台、WebAuth 认证页与用户门户
- 修复：管理后台浏览器标题与网站图标未跟随品牌设置
- 优化：自定义 CSS 帮助文档补充 scoped 样式需加 !important 的说明

## v0.15.1-beta

- RemLink 二进制发布包
